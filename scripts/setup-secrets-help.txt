    --help|-h)
        echo "Usage: $0"
        echo ""
        echo "Environment variables:"
        echo "  DEPLOY_HOST           SSH config host or hostname (default: scoutlgs_lan)"
        echo "  PROJECT_DIR           Remote project directory (default: /home/deploy/mtg-scraper)"
        echo ""
        echo "SSH Configuration:"
        echo "  This script uses SSH config hosts defined in ~/.ssh/config"
        echo "  Default host: scoutlgs_lan (192.168.50.196)"
        echo ""
        echo "  SSH config file (~/.ssh/config) should contain:"
        echo "    Host scoutlgs_lan"
        echo "      HostName 192.168.50.196"
        echo "      User deploy"
        echo "      IdentityFile ~/.ssh/scoutlgs_deploy_key"
        echo ""
        echo "  Generate SSH key:"
        echo "    ssh-keygen -t ed25519 -C \"scoutlgs-deployment\" -f ~/.ssh/scoutlgs_deploy_key"
        echo "  Copy to server:"
        echo "    ssh-copy-id -i ~/.ssh/scoutlgs_deploy_key.pub deploy@192.168.50.196"
        echo ""
        echo "Examples:"
        echo "  # Use default (scoutlgs_lan)"
        echo "  pnpm secrets:setup"
        echo ""
        echo "  # Use specific SSH config host"
        echo "  DEPLOY_HOST=scoutlgs_prod pnpm secrets:setup"
        echo ""
        echo "  # Use direct hostname/IP"
        echo "  DEPLOY_HOST=192.168.1.100 pnpm secrets:setup"
        exit 0
        ;;
